'use strict';

/**
 * authentication-link controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::authentication-link.authentication-link');
