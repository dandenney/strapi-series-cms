'use strict';

/**
 * authentication-link router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::authentication-link.authentication-link');
