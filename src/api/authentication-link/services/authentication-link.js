'use strict';

/**
 * authentication-link service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::authentication-link.authentication-link');
